# 安装指南

本指南详细介绍如何在不同环境中安装和配置 Brain-Inspired AI Framework。

## 📋 系统要求

### 最低要求
- **操作系统**: Windows 10+, macOS 10.14+, Ubuntu 18.04+
- **Python**: 3.8+ (推荐 3.9-3.11)
- **内存**: 4GB RAM (推荐 8GB+)
- **磁盘空间**: 5GB (推荐 10GB+)
- **处理器**: 双核处理器 (推荐四核+)

### 推荐配置
- **操作系统**: Ubuntu 20.04+ / macOS 12+ / Windows 11
- **Python**: 3.10-3.11
- **内存**: 16GB+ RAM
- **磁盘空间**: 20GB+ SSD
- **处理器**: Intel i5/AMD Ryzen 5+ (8核+)
- **GPU**: NVIDIA RTX 3060+ (用于GPU加速)

### GPU支持
- **NVIDIA GPU**: CUDA 11.8+ (推荐 CUDA 12.0+)
- **显存**: 6GB+ (推荐 12GB+)
- **驱动**: NVIDIA Driver 520+ (推荐最新)

## 🚀 快速安装

### 方法1: 使用pip (推荐)

```bash
# 安装最新稳定版本
pip install brain-inspired-ai

# 验证安装
python -c "import brain_ai; print(brain_ai.__version__)"
```

### 方法2: 从源码安装

```bash
# 克隆仓库
git clone https://github.com/brain-ai/brain-inspired-ai.git
cd brain-inspired-ai

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Linux/macOS
source venv/bin/activate
# Windows
venv\Scripts\activate

# 安装依赖
pip install --upgrade pip
pip install -r requirements.txt

# 安装项目
pip install -e .

# 验证安装
python main.py info
```

## 🔧 详细安装过程

### Windows安装

#### 1. 安装Python

```bash
# 从 python.org 下载Python 3.10+
# 或使用 Chocolatey
choco install python310

# 验证安装
python --version
```

#### 2. 创建虚拟环境

```powershell
# 创建项目目录
mkdir brain-ai-project
cd brain-ai-project

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
venv\Scripts\activate

# 升级pip
python -m pip install --upgrade pip
```

#### 3. 安装依赖

```powershell
# 克隆项目
git clone https://github.com/brain-ai/brain-inspired-ai.git
cd brain-inspired-ai

# 安装基础依赖
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
pip install -r requirements.txt

# 安装项目
pip install -e .
```

#### 4. 验证安装

```powershell
python main.py demo --demo-type basic
```

### macOS安装

#### 1. 使用Homebrew安装依赖

```bash
# 安装Homebrew (如果没有)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装Python和依赖
brew install python@3.10
brew install git
brew install cmake
brew install pkg-config
```

#### 2. 创建虚拟环境

```bash
# 创建项目目录
mkdir brain-ai-project
cd brain-ai-project

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级pip
pip install --upgrade pip
```

#### 3. 安装PyTorch (M1 Mac)

```bash
# Apple Silicon Mac (M1/M2)
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Intel Mac
pip install torch torchvision
```

#### 4. 完整安装

```bash
# 克隆项目
git clone https://github.com/brain-ai/brain-inspired-ai.git
cd brain-inspired-ai

# 安装所有依赖
pip install -r requirements.txt

# 安装项目
pip install -e .

# 验证安装
python main.py demo --demo-type basic
```

### Linux安装

#### Ubuntu 20.04/22.04

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Python和开发工具
sudo apt install -y python3 python3-pip python3-venv python3-dev
sudo apt install -y git build-essential cmake pkg-config

# 创建虚拟环境
mkdir brain-ai-project
cd brain-ai-project
python3 -m venv venv
source venv/bin/activate

# 升级pip
pip install --upgrade pip

# 克隆项目
git clone https://github.com/brain-ai/brain-inspired-ai.git
cd brain-inspired-ai

# 安装PyTorch (CPU版本)
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# 安装其他依赖
pip install -r requirements.txt

# 安装项目
pip install -e .
```

#### CentOS/RHEL

```bash
# 安装开发工具
sudo yum groupinstall -y "Development Tools"
sudo yum install -y python3 python3-pip python3-devel git

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
pip install -r requirements.txt

# 安装项目
pip install -e .
```

## 🎮 GPU安装

### NVIDIA GPU支持

#### 1. 安装NVIDIA驱动

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install -y nvidia-driver-520

# 验证驱动安装
nvidia-smi
```

#### 2. 安装CUDA Toolkit

```bash
# 下载并安装CUDA 12.0
wget https://developer.download.nvidia.com/compute/cuda/12.0.0/local_installers/cuda_12.0.0_525.60.13_linux.run
sudo sh cuda_12.0.0_525.60.13_linux.run

# 添加CUDA到PATH
echo 'export PATH=/usr/local/cuda/bin:$PATH' >> ~/.bashrc
echo 'export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH' >> ~/.bashrc
source ~/.bashrc

# 验证CUDA安装
nvcc --version
```

#### 3. 安装PyTorch GPU版本

```bash
# 创建新的虚拟环境
python -m venv gpu-env
source gpu-env/bin/activate

# 安装PyTorch GPU版本
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# 验证GPU支持
python -c "import torch; print(f'CUDA available: {torch.cuda.is_available()}'); print(f'GPU count: {torch.cuda.device_count()}')"
```

#### 4. 安装完整项目

```bash
# 安装依赖
pip install -r requirements.txt

# 安装项目
pip install -e ".[gpu]"

# 测试GPU性能
python main.py train --model-type hippocampus --device cuda --epochs 5
```

### AMD GPU支持 (实验性)

```bash
# 安装ROCm (AMD GPU计算平台)
# 参考: https://rocm.docs.amd.com/

# 安装PyTorch ROCm版本
pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm5.4

# 验证安装
python -c "import torch; print(f'ROCm available: {torch.cuda.is_available()}')"
```

## 🐳 Docker安装

### 使用Docker Compose (推荐)

```bash
# 克隆项目
git clone https://github.com/brain-ai/brain-inspired-ai.git
cd brain-inspired-ai

# 使用docker-compose启动
docker-compose up -d

# 进入容器
docker-compose exec brain-ai bash

# 运行演示
python main.py demo --demo-type basic
```

### 手动构建镜像

```bash
# 构建CPU版本镜像
docker build -t brain-ai:cpu -f Dockerfile.cpu .

# 构建GPU版本镜像
docker build -t brain-ai:gpu -f Dockerfile.gpu .

# 运行容器
# CPU版本
docker run -it --rm brain-ai:cpu python main.py demo --demo-type basic

# GPU版本
docker run -it --rm --gpus all brain-ai:gpu python main.py demo --demo-type basic
```

### 绑定挂载数据

```bash
# 挂载本地目录到容器
docker run -it --rm \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/output:/app/output \
  brain-ai:cpu python main.py demo --demo-type advanced
```

## ☁️ 云平台安装

### Google Colab

```python
# 1. 创建新的Colab笔记本
# 2. 安装依赖
!pip install brain-inspired-ai

# 3. 验证安装
import brain_ai
print(f"版本: {brain_ai.__version__}")

# 4. 运行示例
!python -c "from brain_ai import HippocampalSimulator; print('海马体模块导入成功')"
```

### AWS EC2

```bash
# 1. 启动EC2实例 (推荐: g4dn.xlarge for GPU)
# 2. 连接实例
ssh -i your-key.pem ubuntu@your-ec2-ip

# 3. 安装系统依赖
sudo apt update
sudo apt install -y python3 python3-pip git

# 4. 克隆并安装项目
git clone https://github.com/brain-ai/brain-inspired-ai.git
cd brain-inspired-ai
pip install -e ".[gpu]"

# 5. 运行测试
python main.py demo --demo-type basic
```

### Azure ML

```python
# 1. 创建Azure ML工作区
# 2. 提交训练作业
from azureml.core import Workspace, Experiment
from azureml.train.estimator import Estimator

ws = Workspace.from_config()
experiment = Experiment(workspace=ws, name="brain-ai-training")

estimator = Estimator(
    source_directory='.',
    entry_script='scripts/train.py',
    compute_target='cpu-cluster',
    pip_packages=['brain-inspired-ai']
)

run = experiment.submit(estimator)
```

## 🔧 开发环境安装

### 完整开发环境

```bash
# 克隆项目
git clone https://github.com/brain-ai/brain-inspired-ai.git
cd brain-inspired-ai

# 创建开发虚拟环境
python -m venv dev-env
source dev-env/bin/activate

# 安装开发依赖
pip install -e ".[dev,test,docs,visualization,gpu]"

# 安装pre-commit钩子
pre-commit install

# 运行代码质量检查
black src/ tests/
isort src/ tests/
flake8 src/ tests/
mypy src/

# 运行测试
pytest tests/ --cov=brain_ai --cov-report=html
```

### IDE配置

#### VSCode设置

```json
// .vscode/settings.json
{
  "python.defaultInterpreterPath": "./dev-env/bin/python",
  "python.linting.enabled": true,
  "python.linting.pylintEnabled": true,
  "python.formatting.provider": "black",
  "python.sortImports.args": ["--profile", "black"],
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.organizeImports": true
  },
  "python.testing.pytestEnabled": true,
  "python.testing.pytestArgs": ["tests/"],
  "files.exclude": {
    "**/__pycache__": true,
    "**/*.pyc": true,
    "**/.pytest_cache": true,
    "**/htmlcov": true
  }
}
```

#### PyCharm配置

```python
# 1. 打开项目
# 2. 设置解释器: File > Settings > Project > Python Interpreter
# 3. 选择dev-env中的Python解释器
# 4. 安装插件: Python, Git Integration
# 5. 配置代码风格: File > Settings > Tools > External Tools
```

## ❗ 故障排除

### 常见安装问题

#### 1. Python版本冲突

```bash
# 错误: Python 3.8 is required
# 解决方案:
# 检查Python版本
python --version
python3 --version

# 如果版本不对，安装正确版本
# Ubuntu/Debian
sudo apt install python3.10

# 创建符号链接
sudo ln -sf /usr/bin/python3.10 /usr/bin/python3
```

#### 2. CUDA版本不匹配

```bash
# 错误: CUDA version mismatch
# 解决方案:
# 检查CUDA版本
nvcc --version
nvidia-smi

# 卸载并重新安装PyTorch
pip uninstall torch torchvision
pip install torch==2.0.0+cu118 -f https://download.pytorch.org/whl/torch_stable.html

# 验证安装
python -c "import torch; print(torch.cuda.is_available())"
```

#### 3. 内存不足

```bash
# 错误: MemoryError during installation
# 解决方案:
# 使用更少的并行进程
pip install --no-cache-dir -r requirements.txt

# 分批安装大包
pip install torch
pip install -r requirements.txt --no-deps

# 使用conda替代pip
conda install -c conda-forge brain-inspired-ai
```

#### 4. 权限问题

```bash
# 错误: Permission denied
# 解决方案:
# 使用用户安装
pip install --user brain-inspired-ai

# 或使用虚拟环境
python -m venv venv
source venv/bin/activate
pip install brain-inspired-ai
```

### 性能问题诊断

#### GPU性能检测

```python
# 检测GPU可用性
import torch
print(f"CUDA available: {torch.cuda.is_available()}")
print(f"GPU count: {torch.cuda.device_count()}")
print(f"GPU name: {torch.cuda.get_device_name(0)}")
print(f"GPU memory: {torch.cuda.get_device_properties(0).total_memory}")

# 性能基准测试
import time
x = torch.randn(1000, 1000).cuda()
start = time.time()
result = x @ x
end = time.time()
print(f"Matrix multiplication time: {end - start:.4f}s")
```

#### 内存使用监控

```bash
# 监控内存使用
top -p $(pgrep python)

# 详细内存分析
python -c "
import psutil
process = psutil.Process()
print(f'内存使用: {process.memory_info().rss / 1024 / 1024:.2f} MB')
print(f'内存百分比: {process.memory_percent():.2f}%')
"
```

## 📚 升级指南

### 从旧版本升级

```bash
# 备份当前配置
cp -r ~/.brain-ai ~/.brain-ai.backup

# 升级到最新版本
pip install --upgrade brain-inspired-ai

# 运行迁移脚本
python scripts/migrate.py --from-version 0.9 --to-version 1.0

# 验证升级
python main.py info
```

### 版本兼容性

| 版本 | Python要求 | PyTorch要求 | 向后兼容 |
|------|------------|-------------|----------|
| 1.0.x | 3.8+ | 2.0+ | ✅ |
| 0.9.x | 3.8+ | 1.13+ | ⚠️ |
| 0.8.x | 3.7+ | 1.10+ | ❌ |

## 🆘 获取帮助

### 调试模式

```bash
# 启用详细日志
export BRAIN_AI_LOG_LEVEL=DEBUG
python main.py demo --demo-type basic

# 收集诊断信息
python scripts/diagnostic.py > diagnostic.log 2>&1
```

### 社区支持

- **GitHub Issues**: [报告问题和bug](https://github.com/brain-ai/brain-inspired-ai/issues)
- **Discord社区**: [实时聊天支持](https://discord.gg/brain-ai)
- **邮件支持**: team@brain-ai.org
- **Stack Overflow**: 标签 `brain-inspired-ai`

### 官方资源

- **文档网站**: https://docs.brain-ai.org
- **API参考**: https://api.brain-ai.org
- **教程视频**: https://youtube.com/brain-ai
- **研究论文**: https://arxiv.org/brain-ai

---

**恭喜！现在您已经成功安装了 Brain-Inspired AI Framework。** 🎉

接下来请阅读 [快速开始](快速开始.md) 了解如何开始使用！